#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string to count its length
 * Return: the string lenth
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}

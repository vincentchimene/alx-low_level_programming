#include <stdio.h>
/* A program that prints the alphabet in lowercase, followed by a new line.*/
/**
 * main - main block
 * Return: 0
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
		putchar(\n);
	return (0);
}
